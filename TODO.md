# TODO - Nhấn mạnh “từ đặc sắc” bằng màu sắc (Gradient + glow)

- [x] Gắn marker từ/cụm từ đặc sắc trong `index.html` bằng `<span class="text-accent">...</span>` ở Hero/About.
- [x] Chuẩn bị TODO tổng hợp.

- [x] Thêm CSS cho `.text-accent` trong `styles.css` (gradient + glow, hỗ trợ dark mode).

- [ ] Cập nhật `app.js` để highlight từ khóa trong modal dự án (từ dữ liệu mô tả/tính năng).
- [ ] Xác nhận TODO hoàn tất sau khi highlight modal chạy đúng.

- [ ] Mở `index.html` chạy thử: kiểm tra hiển thị ở light/dark + modal.

